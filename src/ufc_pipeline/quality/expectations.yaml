bronze_events:
  key: event_id
  required: ["event_id", "name", "event_url", "event_date", "status"]

bronze_fights:
  key: fight_id
  required: ["fight_id", "event_id", "fight_url", "fighters"]

bronze_fighters:
  key: fighter_id
  required: ["fighter_id", "name", "profile_url"]
